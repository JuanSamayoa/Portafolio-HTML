module.exports = {
  plugins: {
    // Tailwind v4 is integrated via the Vite plugin; keep autoprefixer only if
    // you need additional vendor-prefixing compatibility.
    autoprefixer: {},
  },
};
