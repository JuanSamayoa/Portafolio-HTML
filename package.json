{
  "name": "portafolio-dev",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build --outDir=docs && echo. > docs/.nojekyll && copy public\\404.html docs\\ 2>nul || echo No 404.html found in public && echo juan-samayoa.is-a.dev > docs/CNAME && git add . && git commit -m \"deploy: auto deploy update\" && git push origin main"
  },
  "dependencies": {
    "@fontsource/onest": "^5.2.7",
    "astro": "^5.6.1"
  },
  "devDependencies": {
    "@astrojs/tailwind": "^5.1.0",
    "tailwindcss": "^3.4.1"
  }
}
